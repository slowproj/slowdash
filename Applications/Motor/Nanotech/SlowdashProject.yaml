slowdash_project:
  name: Nanotech_Motor

  environment:
    - DB_URL=${DB_URL:-sqlite:///SlowMotor}

  data_source:
    url: ${DB_URL}
    time_series:
      schema: data [channel] @timestamp(unix) = value

  task:
    name: NanotechMotor
    auto_load: true
    parameters:
      #IP: "192.168.50.176"
      MAC: "44:aa:e8:00:1f:f4"
      firmware_version: 1825
      db_url: ${DB_URL}
